---
description: AgentHub AI 协作运行规则
globs: ["ai-collab/**/*.md", "docs/**/*.md", "README.md", "**/*.py", "**/*.ts", "**/*.tsx"]
alwaysApply: true
---

# AI 协作运行规则

这些规则约束需求澄清、文档维护、记录沉淀和交付反馈。它们适用于人类开发者与 AI 共同推进 AgentHub 的所有工作。

## R-C-1 全部协作文档使用中文

- 除路径、命令、代码标识符、协议字段、错误码外，叙述性内容必须使用中文。
- 不保留乱码、半截翻译或无法执行的模糊表述。
- 修改文档时必须保持术语统一：会话、消息、Agent、Adapter、Artifact、Orchestrator、Trace。

## R-C-2 先规格，后实现

- 新功能必须先映射到 `ai-collab/SPEC.md` 的 Feature。
- 没有验收标准的任务不得直接进入实现。
- 如果实现过程中发现规格不对，先更新规格并说明取舍，再继续实现。

## R-C-3 Skill 是可复用 SOP，不是流水账

- Skill 必须包含使用场景、预读材料、操作步骤、测试方式和自检清单。
- 只有一次性的过程记录放入 `records/`，不要放入 `skills/`。
- 当同类问题第二次出现时，必须把解决步骤沉淀为 Skill。

## R-C-4 Rule 是硬约束，不写建议语气

- Rule 必须写"必须""禁止""不得"，避免"建议""最好"。
- Rule 只沉淀长期有效且可检查的工程约束。
- 每条规则必须能在 `records/` 中找到真实来源（踩坑记录或决策片段）。

## R-C-5 Records 保持真实

- `records/` 保留当时上下文、判断和结果，不做事后美化。
- 修正历史结论时追加新段落或新文件，不覆盖原始事实。
- 记录必须能回答：为什么这样做、AI 提供了什么、人工如何决策、结果如何验证。

## R-C-6 交付反馈必须可验证

- 最终反馈必须说明改了哪些文档、运行了哪些检查、哪些风险仍存在。
- 未运行测试或未验证的内容必须明确说出原因。
- 不得把 `Planned` 或 `In Progress` 描述成已经完成。

## R-C-7 禁止 AI 擅自扩展范围

- AI 不得实现 SPEC 未要求的功能、UI 或配置项。
- AI 不得重构与当前任务无关的模块。
- AI 不得覆盖用户未明确要求修改的已有代码。
- 如果 AI 认为某处需要额外改动，必须先向操作员说明理由并等待确认。
- 来源：W2-D1 片段 3.2（AI 提议加"刷新"按钮被拒绝）。

## R-C-8 Service 层不得依赖传输层

- 业务服务（`server/services/`）不得导入或使用 HTTP 特定类型（如 `HTTPException`）。
- 服务层通过 `ValueError(code)` 表达业务错误，由 REST/WS 层统一映射为传输协议错误。
- 来源：W2-D1 决策 1（service 抛 code vs REST 抛 HTTPException 的取舍）。

## R-C-9 归档不是搬运，是重新组织

- 归档文档的读者目的与原文档不同，措辞和结构必须相应调整。
- 归档时必须包含"迁移对照"，说明旧模块在新架构中的对应位置或替代方案。
- 来源：D0 片段 3.2（v1-terminal.md 需要迁移对照表而非 1:1 复制）。

## R-C-10 规则必须有来源，记录必须有结论

- 每条 `rules/` 中的硬约束必须能从 `records/` 中找到至少一处真实踩坑记录。
- 每份 `records/` 必须包含：目标、上下文、AI 建议、人类决策、执行结果、踩坑、规则或 Skill 更新。
- 当规则不再适用时，必须在记录中说明废弃原因，然后移除规则。
