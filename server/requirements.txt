# AgentHub BFF dependencies
# Pin lower bounds only; CI / 锁文件后续再上。

# --- runtime ---
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
sqlalchemy>=2.0
aiosqlite>=0.20
python-multipart

# --- dev / test ---
pytest>=8.0
pytest-asyncio>=0.23
httpx>=0.27
